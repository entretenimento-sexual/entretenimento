// ================================================================
// ROOMS.RULES
// - Coleção raiz: rooms/{roomId}
// - Read: participantes ou owner
// - Update normal:
//   - owner pode alterar detalhes do room e participants
//   - participante comum só altera lastActivity/lastMessage
// - Join controlado por convite:
//   - exige invite root com docId canônico room:<roomId>:to:<uid>
//   - status pending
//   - receiverId == uid()
//   - targetId/roomId apontando para a própria sala
//   - expiresAt > request.time
// ✅ ARQUIVO MODULAR: NÃO pode ter `rules_version` nem `service cloud.firestore`
// uso de helpers globais
// firestore-rules/rooms.rules
// ================================================================

match /rooms/{roomId} {
  function participantsFrom(data) {
    return (data.keys().hasAny(["participants"]) && data.participants is list)
      ? data.participants
      : [];
  }

  function isRoomParticipant() {
    return signedIn() && (uid() in participantsFrom(resource.data));
  }

  function isRoomOwner() {
    return signedIn()
      && resource.data.keys().hasAny(["createdBy"])
      && resource.data.createdBy == uid();
  }

  // ------------------------------------------------------------
  // JOIN BY INVITE (root invites)
  // DocId canônico: invites/room:<roomId>:to:<uid>
  // ------------------------------------------------------------
  function joinInviteId() {
    return "room:" + roomId + ":to:" + uid();
  }

  function joinInvitePath() {
    return /databases/$(database)/documents/invites/$(joinInviteId());
  }

  function joinInviteExists() {
    return exists(joinInvitePath());
  }

  function joinInviteData() {
    return get(joinInvitePath()).data;
  }

  function joinInviteIsRoomType() {
    return !joinInviteData().keys().hasAny(["type"])
      || joinInviteData().type == "room";
  }

  function joinInviteTargetsThisRoom() {
    return (
      (joinInviteData().keys().hasAny(["targetId"]) && joinInviteData().targetId == roomId)
      || (joinInviteData().keys().hasAny(["roomId"]) && joinInviteData().roomId == roomId)
    );
  }

  function hasPendingJoinInvite() {
    return signedIn()
      && joinInviteExists()
      && joinInviteData().status == "pending"
      && joinInviteData().receiverId == uid()
      && joinInviteIsRoomType()
      && joinInviteTargetsThisRoom()
      && joinInviteData().expiresAt > request.time;
  }

  function isJoinUpdateShape() {
    let oldParticipants = participantsFrom(resource.data);

    return signedIn()
      && !(uid() in oldParticipants)
      && request.resource.data.diff(resource.data).changedKeys().hasOnly([
        "participants",
        "lastActivity"
      ])
      && (request.resource.data.participants is list)
      && (uid() in request.resource.data.participants)
      && request.resource.data.participants.size() == oldParticipants.size() + 1
      && request.resource.data.participants.hasAll(oldParticipants)
      && request.resource.data.lastActivity == request.time;
  }

  allow read: if isRoomParticipant() || isRoomOwner();

  allow create: if signedIn()
    && request.resource.data.keys().hasAll([
      "createdBy",
      "participants",
      "roomName",
      "creationTime"
    ])
    && request.resource.data.createdBy == uid()
    && request.resource.data.participants is list
    && (uid() in request.resource.data.participants)
    && isStr(request.resource.data.roomName)
    && isTs(request.resource.data.creationTime);

  allow update: if
    (
      (isRoomParticipant() || isRoomOwner())
      && (
        (isRoomOwner()
          && request.resource.data.diff(resource.data).changedKeys().hasOnly([
            "roomName",
            "description",
            "expirationDate",
            "maxParticipants",
            "isPrivate",
            "roomType",
            "visibility",
            "participants",
            "lastActivity",
            "lastMessage"
          ])
        )
        ||
        (!isRoomOwner()
          && request.resource.data.diff(resource.data).changedKeys().hasOnly([
            "lastActivity",
            "lastMessage"
          ])
        )
      )
    )
    ||
    (
      hasPendingJoinInvite()
      && isJoinUpdateShape()
    );

  allow delete: if false;
}
// ================================================================
//                        FIM DO ROOMS.RULES
// ================================================================
