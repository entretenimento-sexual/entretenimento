/* To learn more about this file see: https://angular.io/config/tsconfig. */
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": ["@angular/localize"],
    "lib": ["ES2022", "DOM", "DOM.Iterable"],   // ← redundante, mas força no alvo
    "useDefineForClassFields": false
  },
  "files": ["src/main.ts"],
  "include": ["src/**/*.ts"],
  "exclude": [
    "src/**/*.spec.ts",
    "src/test/**/*.ts"
  ]
}
