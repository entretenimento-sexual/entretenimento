name: Community visual contract maintenance

on:
  push:
    branches:
      - main
    paths:
      - '.github/scripts/community-visual-contract-maintenance.py'
      - '.github/workflows/community-visual-contract-maintenance.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Apply visual contract coverage
        shell: bash
        run: |
          set -euo pipefail
          python3 .github/scripts/community-visual-contract-maintenance.py
          git diff --check
          git rm .github/scripts/community-visual-contract-maintenance.py
          git rm .github/workflows/community-visual-contract-maintenance.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add .github/visual-harness/community-preview-visual.app-component.ts
          git add .github/scripts/community-preview-visual-validation.sh
          git commit -m 'test(community): protect page visual hierarchy contract'
          git push origin HEAD:main
