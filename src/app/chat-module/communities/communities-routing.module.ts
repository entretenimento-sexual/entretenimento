// src/app/communities/communities-routing.module.ts
// Placeholder do módulo de comunidades.
// Enquanto não houver rotas reais, manter explícito ajuda a evitar
// decisões implícitas e confusão de navegação.
import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';

const routes: Routes = [
  // Futuras rotas de comunidades entram aqui.
];

@NgModule({
  imports: [RouterModule.forChild(routes)],
  exports: [RouterModule]
})
export class CommunitiesRoutingModule { }
