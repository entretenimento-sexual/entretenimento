// src/app/photo-editor/photo-editor-routing.module.ts
// Placeholder do módulo de edição de fotos.
// Se o módulo não tiver navegação própria, isso pode continuar vazio por enquanto.
import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';

const routes: Routes = [
  // Futuras rotas do editor entram aqui.
];

@NgModule({
  imports: [RouterModule.forChild(routes)],
  exports: [RouterModule]
})
export class PhotoEditorRoutingModule { }
